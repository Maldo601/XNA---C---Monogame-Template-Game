public enum ComponentType
{
    Sprite,
    PlayerInput,
    Animation
}

public enum Input
{
    Left,
    Right,
    Up,
    Down,


    None
}

public enum GameState
{
    StartMenu,
    Loading,
    Playing,
    Paused
}

public enum Direction
{
    Left,
    Right,
    Up,
    Down,
    None
}

public enum State
{
    Standing, 
    Walking
}
